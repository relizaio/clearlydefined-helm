1. Get the application URL:
{{- if or .Values.useTraefikLe .Values.traefikBehindLb }}
  {{ .Values.projectProtocol | default "http" }}://{{ .Values.leHost }}
{{- else }}
  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "clearlydefined.fullname" . }}-service {{ .Values.service.service.port }}:{{ .Values.service.service.port }}
  Then visit: http://127.0.0.1:{{ .Values.service.service.port }}
{{- end }}

2. ClearlyDefined Service API is available at:
   - Internal: http://{{ include "clearlydefined.fullname" . }}-service:{{ .Values.service.service.port }}

3. Crawler API is available at:
   - Internal: http://{{ include "clearlydefined.fullname" . }}-crawler:{{ .Values.crawler.service.port }}

4. MongoDB is available at:
   - Internal: {{ include "clearlydefined.fullname" . }}-mongodb:{{ .Values.mongodb.service.port }}

5. Redis is available at:
   - Internal: {{ include "clearlydefined.fullname" . }}-redis:{{ .Values.redis.service.port }}

IMPORTANT: This chart requires an existing Kubernetes secret "{{ .Values.existingSecret }}"
containing at minimum:
  - CURATION_GITHUB_TOKEN
  - CRAWLER_GITHUB_TOKEN

Create it before installing:
  kubectl create secret generic {{ .Values.existingSecret }} \
    --namespace {{ .Release.Namespace }} \
    --from-literal=CURATION_GITHUB_TOKEN="your-github-token" \
    --from-literal=CRAWLER_GITHUB_TOKEN="your-github-token"

For more information, visit: https://docs.clearlydefined.io
