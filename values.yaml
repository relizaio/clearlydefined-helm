# Default values for clearlydefined
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Global configuration
global:
  storageClass: ""

# Image configuration
images:
  service:
    repository: registry.relizahub.com/83d27192-7d06-4d01-a5d6-de4839926da2-public/clearlydefined-service
    pullPolicy: Always
  crawler:
    repository: registry.relizahub.com/83d27192-7d06-4d01-a5d6-de4839926da2-public/clearlydefined-crawler
    pullPolicy: Always
  mongodb:
    repository: docker.io/library/mongo8.2.5-noble@sha256:6458b950c6340fb522868118b5cd31b52dad4e2fc7bf341e5732babebc9ae005
    pullPolicy: IfNotPresent
  mongoSeed:
    # this is disabled by default
    repository: clearlydefined/docker_dev_env_experiment_clearlydefined_mongo_seed
    pullPolicy: IfNotPresent
  redis:
    repository: docker.io/library/redis:8.6.1-alpine3.23@sha256:2afba59292f25f5d1af200496db41bea2c6c816b059f57ae74703a50a03a27d0
    pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

# Service component configuration
service:
  enabled: true
  replicaCount: 1
  
  service:
    type: ClusterIP
    port: 4000
    targetPort: 4000
  
  resources:
    requests:
      cpu: 500m
      memory: 512Mi
  
  nodeSelector: {}
  tolerations: []
  affinity: {}
  
  # Debug configuration
  debug:
    enabled: false
    port: 9230

# Crawler component configuration
crawler:
  enabled: true
  replicaCount: 1
  
  service:
    type: ClusterIP
    port: 5000
    targetPort: 5000
  
  resources:
    requests:
      cpu: 750m
      memory: 512Mi
  
  nodeSelector: {}
  tolerations: []
  affinity: {}
  
  # Debug configuration
  debug:
    enabled: false
    port: 9229

# MongoDB configuration
mongodb:
  enabled: true
  
  service:
    type: ClusterIP
    port: 27017
  
  persistence:
    enabled: true
    size: 10Gi
    accessMode: ReadWriteOnce
    # storageClass: ""
  
  resources:
    requests:
      cpu: 250m
      memory: 512Mi
  
  nodeSelector: {}
  tolerations: []
  affinity: {}

# MongoDB Seed Job configuration
# Note: This is for development/testing with sample data only.
# If using your own curated data repository, set enabled: false
mongoSeed:
  enabled: false
  
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 200m
      memory: 256Mi

# Redis configuration
redis:
  enabled: true
  
  service:
    type: ClusterIP
    port: 6379
  
  persistence:
    enabled: true
    size: 5Gi
    accessMode: ReadWriteOnce
    # storageClass: ""
  
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 250m
      memory: 512Mi
  
  nodeSelector: {}
  tolerations: []
  affinity: {}

# Shared harvested data volume
# Note: Using ReadWriteOnce - service and crawler must be scheduled on the same node
harvestedData:
  persistence:
    enabled: true
    size: 20Gi
    accessMode: ReadWriteOnce
    # storageClass: ""

# Environment configuration
# Non-sensitive configuration values
config:
  # Curation GitHub Info
  curation:
    github:
      branch: "master"
      owner: "clearlydefined"
      repo: "curated-data-dev"
    provider: "github"
    store:
      provider: "mongo"
      connectionString: "mongodb://clearlydefined-mongodb:27017"
      dbName: "clearlydefined"
      collectionName: "curations"
  
  # Definition Store Info
  definition:
    store:
      provider: "mongo"
      connectionString: "mongodb://clearlydefined-mongodb:27017"
      dbName: "clearlydefined"
      collectionName: "definitions-paged"
  
  # Harvest Store Info
  harvest:
    store:
      provider: "file"
  
  # File Store Location
  fileStore:
    location: "/tmp/harvested_data"
  
  # Crawler Info
  crawler:
    apiUrl: "http://clearlydefined-crawler:5000"
    deadletterProvider: "cd(file)"
    name: "cdcrawlerlocal"
    queueProvider: "memory"
    storeProvider: "cd(file)"
    # Optional: Set crawler host and webhook URL if needed
    host: ""
    webhookUrl: ""
    queuePrefix: ""
    azblob:
      containerName: ""

# Secrets configuration
# Reference an existing Kubernetes secret that contains sensitive environment variables.
# The secret must be created before installing the chart.
# See INSTALL.md for instructions on creating the secret.
existingSecret: "clearlydefined-secrets"

# Traefik Ingress configuration
# Enable one of: useTraefikLe (with Let's Encrypt) or traefikBehindLb (behind load balancer)
useTraefikLe: false
traefikBehindLb: false

# Traefik configuration
traefik_crd_api_version: "traefik.containo.us/v1alpha1"
leHost: "clearlydefined.local"
projectProtocol: "https"  # Set to "https" to enable HTTP to HTTPS redirect

serviceAccount:
  create: true
  annotations: {}
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000
